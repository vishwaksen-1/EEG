main.m, main1.m, main3.m:
    Script files to see channel-wise bootstrapped (active-passive) signal[Mean +- CI]
    across normalizations

Main2CNormDiff.m:
    Script to see all 3 normalizations overlaid just so to note if the
    normalizations produced same signal or are they different.
